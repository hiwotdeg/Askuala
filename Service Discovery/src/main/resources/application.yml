eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761

spring:
  banner:
    location: classpath:banner.txt


#Tracing
management:
  tracing:
    sampling:
      probability: 1.0
